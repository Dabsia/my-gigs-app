export const API_BASE_URL = "http://172.31.136.191:5000";

/** Base height of the bottom tab bar (without safe area). Use with safe area inset for scroll padding. */
export const TAB_BAR_BASE_HEIGHT = 80;
